<!-- Desktop wrapper con sidebar -->
<div class="navigation-container top" data-tabs-section>
  <!-- Mobile Burger Menu -->
  <div class="mobile-header">
    <button class="burger-menu" onclick="toggleMobileFooter()" aria-label="Apri menu">
      <span></span>
      <span></span>
      <span></span>
    </button>
  </div>

  <!-- Include sidebar navigation -->
  {% assign current_page_title = 'Biglietti' %}
  {% render 'sidebar-navigation' %}

  <!-- Content Area -->
  <div class="content-wrapper">
    <div class="content show">
      <div class="formatted-content">
        <h1>Biglietti e Prenotazioni</h1>
        <p>Acquista il tuo biglietto online per garantirti l'accesso alle <strong>Lucine di Natale 2025</strong> con prezzo agevolato e posto riservato.</p>

        <!-- Main CTA Top -->
        <section class="location-section">
          <div class="sketch-card sketch seed-6" class="text-center">
            <svg class="sketch-border" viewbox="0 0 100 100" aria-hidden="true" preserveaspectratio="none" class="sketch-border-red">
              <defs>
                <filter id="wobble-6">
                  <feturbulence type="fractalNoise" basefrequency="1.2" numoctaves="1" seed="13"></feturbulence>
                  <fedisplacementmap in="SourceGraphic" scale="1"></fedisplacementmap>
                </filter>
              </defs>
              <rect x="4" y="4" width="92" height="92" rx="12" fill="none" stroke="currentColor" stroke-width="3.2" stroke-linecap="round" stroke-dasharray="16 10 8 14" vector-effect="non-scaling-stroke" filter="url(#wobble-6)"></rect>
            </svg>
            <h3 class="ticket-title">🎟️ Seleziona Data e Ora d'Ingresso</h3>
            <p class="ticket-subtitle">Scegli la data e l'orario che preferisci • Aggiungi al carrello i tuoi biglietti</p>
            <a class="cta cta-primary" href="https://lucinedinatale.it/products/biglietto-parco-lucine-di-natale-2025">Prenota Ora →</a>
            <p class="ticket-meta">Biglietti inviati via email • Conferma immediata</p>
          </div>
        </section>

        <!-- Guida ai Biglietti -->
        <section class="location-section">
          <h3>Guida ai Biglietti</h3>

          <!-- Gratuito -->
          <div class="sketch-card sketch seed-1">
            <svg class="sketch-border" viewbox="0 0 100 100" aria-hidden="true" preserveaspectratio="none">
              <defs>
                <filter id="wobble-gratuito">
                  <feturbulence type="fractalNoise" basefrequency="0.9" numoctaves="1" seed="3"></feturbulence>
                  <fedisplacementmap in="SourceGraphic" scale="1"></fedisplacementmap>
                </filter>
              </defs>
              <rect x="4" y="4" width="92" height="92" rx="12" fill="none" stroke="currentColor" stroke-width="3.2" stroke-linecap="round" stroke-dasharray="14 8 6 12 10 9" vector-effect="non-scaling-stroke" filter="url(#wobble-gratuito)"></rect>
            </svg>
            <h4>👶 Gratuito (0-2 anni)</h4>
            <p class="lead">Per bimbi da 0 a 2 anni</p>
            <p class="meta">Non è necessario l'acquisto del biglietto • L'ingresso viene garantito direttamente all'entrata del parco</p>
          </div>

          <!-- Biglietto Intero -->
          <div class="sketch-card sketch seed-2">
            <svg class="sketch-border" viewbox="0 0 100 100" aria-hidden="true" preserveaspectratio="none">
              <defs>
                <filter id="wobble-intero">
                  <feturbulence type="fractalNoise" basefrequency="0.8" numoctaves="1" seed="5"></feturbulence>
                  <fedisplacementmap in="SourceGraphic" scale="1"></fedisplacementmap>
                </filter>
              </defs>
              <rect x="4" y="4" width="92" height="92" rx="12" fill="none" stroke="currentColor" stroke-width="3.2" stroke-linecap="round" stroke-dasharray="14 8 6 12 10 9" vector-effect="non-scaling-stroke" filter="url(#wobble-intero)"></rect>
            </svg>
            <h4>🎫 Biglietto Intero</h4>
            <p class="lead">N. 1 ingresso nel giorno e nella fascia oraria selezionata</p>
            <p class="meta">Valido per adulti • Accesso standard</p>
          </div>

          <!-- Biglietto Ridotto -->
          <div class="sketch-card sketch seed-3">
            <svg class="sketch-border" viewbox="0 0 100 100" aria-hidden="true" preserveaspectratio="none">
              <defs>
                <filter id="wobble-ridotto">
                  <feturbulence type="fractalNoise" basefrequency="1.1" numoctaves="1" seed="7"></feturbulence>
                  <fedisplacementmap in="SourceGraphic" scale="1"></fedisplacementmap>
                </filter>
              </defs>
              <rect x="4" y="4" width="92" height="92" rx="12" fill="none" stroke="currentColor" stroke-width="3.2" stroke-linecap="round" stroke-dasharray="12 7 5 11 9 8" vector-effect="non-scaling-stroke" filter="url(#wobble-ridotto)"></rect>
            </svg>
            <h4>🎟️ Biglietto Ridotto</h4>
            <p class="lead">N. 1 ingresso nel giorno e nella fascia oraria selezionata</p>
            <p class="meta">Per bambini dai 3 ai 12 anni e disabili • Documentazione richiesta all'ingresso</p>
            <hr class="ticket-divider">
            <p class="meta"><strong>Disabili:</strong> Diritto al biglietto ridotto + n° 1 accompagnatore • Accesso prioritario tramite ingresso separato</p>
          </div>

          <!-- Biglietto Saltafila -->
          <div class="sketch-card sketch seed-4">
            <svg class="sketch-border" viewbox="0 0 100 100" aria-hidden="true" preserveaspectratio="none">
              <defs>
                <filter id="wobble-saltafila">
                  <feturbulence type="fractalNoise" basefrequency="0.7" numoctaves="1" seed="9"></feturbulence>
                  <fedisplacementmap in="SourceGraphic" scale="1"></fedisplacementmap>
                </filter>
              </defs>
              <rect x="4" y="4" width="92" height="92" rx="12" fill="none" stroke="currentColor" stroke-width="3.2" stroke-linecap="round" stroke-dasharray="15 9 7 13" vector-effect="non-scaling-stroke" filter="url(#wobble-saltafila)"></rect>
            </svg>
            <h4>⚡ Biglietto Saltafila</h4>
            <p class="lead">N. 1 ingresso con accesso prioritario</p>
            <p class="meta">Salta-fila all'ingresso del parco • Nel giorno e nella fascia oraria selezionata • Ideale nei weekend</p>
          </div>

          <!-- Biglietto Open -->
          <div class="sketch-card sketch seed-5">
            <svg class="sketch-border" viewbox="0 0 100 100" aria-hidden="true" preserveaspectratio="none">
              <defs>
                <filter id="wobble-open">
                  <feturbulence type="fractalNoise" basefrequency="1.0" numoctaves="1" seed="11"></feturbulence>
                  <fedisplacementmap in="SourceGraphic" scale="1"></fedisplacementmap>
                </filter>
              </defs>
              <rect x="4" y="4" width="92" height="92" rx="12" fill="none" stroke="currentColor" stroke-width="3.2" stroke-linecap="round" stroke-dasharray="13 6 8 10 11 7" vector-effect="non-scaling-stroke" filter="url(#wobble-open)"></rect>
            </svg>
            <h4>🥇 Biglietto Open</h4>
            <p class="lead">Pass esclusivo per accedere quando preferisci</p>
            <p class="meta">Senza necessità di prenotare giorno e orario specifico • Massima flessibilità • Perfetto per regali</p>
          </div>
        </section>

        <!-- Offerte Speciali -->
        <section class="location-section">
          <h3>Offerte Speciali</h3>
          
          <!-- Gruppi e Comitive -->
          <div class="sketch-card sketch seed-static">
            <svg class="sketch-border" viewbox="0 0 100 100" aria-hidden="true" preserveaspectratio="none">
              <rect x="4" y="4" width="92" height="92" rx="12" fill="none" stroke="currentColor" stroke-width="3.2" stroke-linecap="round" stroke-dasharray="16 10 8 14" vector-effect="non-scaling-stroke"></rect>
            </svg>
            <h4>🧑‍🤝‍🧑 Gruppi &amp; Comitive</h4>
            <p class="lead">10% di sconto automatico</p>
            <p class="meta">Per un acquisto minimo di 20 ingressi • Sconto applicato automaticamente al checkout</p>
          </div>

          <!-- Residenti -->
          <div class="sketch-card sketch seed-7">
            <svg class="sketch-border" viewbox="0 0 100 100" aria-hidden="true" preserveaspectratio="none">
              <defs>
                <filter id="wobble-residenti">
                  <feturbulence type="fractalNoise" basefrequency="0.9" numoctaves="1" seed="17"></feturbulence>
                  <fedisplacementmap in="SourceGraphic" scale="1"></fedisplacementmap>
                </filter>
              </defs>
              <rect x="4" y="4" width="92" height="92" rx="12" fill="none" stroke="currentColor" stroke-width="3.2" stroke-linecap="round" stroke-dasharray="14 8 6 12 10 9" vector-effect="non-scaling-stroke" filter="url(#wobble-residenti)"></rect>
            </svg>
            <h4>🏠 Residenti</h4>
            <p class="lead">Prezzo agevolato • Non serve prenotazione</p>
            <p class="meta">Adulti €5 • Bambini €4 • Presentarsi alla cassa con documento attestante la residenza</p>
          </div>
        </section>

        <!-- Main CTA Bottom -->
        <section class="location-section">
          <div class="sketch-card sketch seed-6" class="text-center">
            <svg class="sketch-border" viewbox="0 0 100 100" aria-hidden="true" preserveaspectratio="none" class="sketch-border-red">
              <defs>
                <filter id="wobble-6-bottom">
                  <feturbulence type="fractalNoise" basefrequency="1.2" numoctaves="1" seed="13"></feturbulence>
                  <fedisplacementmap in="SourceGraphic" scale="1"></fedisplacementmap>
                </filter>
              </defs>
              <rect x="4" y="4" width="92" height="92" rx="12" fill="none" stroke="currentColor" stroke-width="3.2" stroke-linecap="round" stroke-dasharray="16 10 8 14" vector-effect="non-scaling-stroke" filter="url(#wobble-6-bottom)"></rect>
            </svg>
            <h3 class="ticket-title">🎟️ Seleziona Data e Ora d'Ingresso</h3>
            <p class="ticket-subtitle">Scegli la data e l'orario che preferisci • Aggiungi al carrello i tuoi biglietti</p>
            <a class="cta cta-primary" href="https://lucinedinatale.it/products/biglietto-parco-lucine-di-natale-2025">Prenota Ora →</a>
            <p class="ticket-meta">Biglietti inviati via email • Conferma immediata</p>
          </div>
        </section>
      </div>
    </div>
  </div>
</div>

<!-- GLOBAL FOOTER - Mobile menu footer -->
{% render 'footer-snap' %}