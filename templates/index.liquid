<!-- LUCINE MINIMAL v1.4 - FIXED TOP POSITION -->
<script>
console.log('🎯 INDEX.LIQUID TEMPLATE LOADED v1.4');
console.log('📋 MENU ITEMS:');
{% assign main_menu = linklists['menu-principale'] %}
{% for link in main_menu.links %}
console.log('{{ forloop.index }}. {{ link.title }} - URL: {{ link.url }}');
{% endfor %}
</script>

<!-- Video Background -->
<div class="video-bg">
  <video autoplay muted loop playsinline>
    <source src="https://cdn.shopify.com/videos/c/o/v/088cf9e084664519ad8bf3af537b87fe.mp4" type="video/mp4">
  </video>
</div>

<!-- Navigation Container -->
<div class="navigation-container" data-tabs-section>
  <!-- Menu -->
  <div class="mobile-menu">
    <div class="menu-tabs">
      <!-- Home sempre primo -->
      <button class="tab" data-index="0">Home</button>
      
      <!-- Menu dinamico da Shopify -->
      {% assign main_menu = linklists['menu-principale'] %}
      {% for link in main_menu.links %}
        <button class="tab" data-index="{{ forloop.index }}" data-url="{{ link.url }}">
          {{ link.title }}
        </button>
      {% endfor %}
    </div>
  </div>

  <!-- Content Area -->
  <div class="content">
    <!-- Home content -->
    <div class="tab-content active" data-index="0">
      <div class="content-section">
        <div class="formatted-content">
          <h1>Lucine di Natale</h1>
          <p class="christmas-accent">Un viaggio magico nel cuore del Natale per <span class="highlight-text">tutta la famiglia</span></p>
          
          <div class="event-card">
            <h3 class="christmas-accent">Benvenuti nel nostro mondo incantato</h3>
            <p>Dal <span class="highlight-text">1 al 24 dicembre</span>, il centro storico si trasforma in un villaggio natalizio dove magia e tradizione si incontrano.</p>
            <p>Spettacoli di luci, laboratori creativi, mercatini artigianali e tanto altro vi aspettano per creare insieme ricordi indimenticabili.</p>
            <a href="#" class="family-cta" onclick="document.querySelector('.tab[data-index=\"2\"]').click(); return false;">Scopri il Programma</a>
          </div>
          
          <div class="info-grid">
            <div class="info-box">
              <h4>📅 Quando</h4>
              <p>1-24 Dicembre<br>15:00 - 21:00</p>
            </div>
            <div class="info-box">
              <h4>📍 Dove</h4>
              <p>Centro Storico<br>Piazza Principale</p>
            </div>
            <div class="info-box">
              <h4>🎟️ Biglietti</h4>
              <p>Da €10<br>Famiglia €40</p>
            </div>
          </div>
        </div>
      </div>
    </div>
    
    <!-- Dynamic content con design system -->
    {% for link in main_menu.links %}
      <div class="tab-content" data-index="{{ forloop.index }}">
        <div class="content-section">
          {% comment %} Contenuto con styling natalizio {% endcomment %}
          {% if link.url contains '/pages/' %}
            {% assign page_handle = link.url | split: '/pages/' | last %}
            {% assign page = pages[page_handle] %}
            
            <!-- DEBUG: Page handle: {{ page_handle }} -->
            <!-- DEBUG: Page exists: {% if page %}YES{% else %}NO{% endif %} -->
            <!-- DEBUG: Content blank: {% if page.content == blank %}YES{% else %}NO{% endif %} -->
            
            {% if page.content != blank %}
              <div class="page-content formatted-content">
                {{ page.content }}
              </div>
              
              {% comment %} Aggiungi CTA se la pagina contiene info specifiche {% endcomment %}
              {% if page_handle contains 'bigliett' or page_handle contains 'ticket' %}
                <div class="event-card" style="margin-top: 2rem; text-align: center;">
                  <h3 class="christmas-accent">Prenota Ora</h3>
                  <p>Non perdere la magia delle <span class="highlight-text">Lucine di Natale</span></p>
                  <a href="#" class="family-cta">Acquista Biglietti</a>
                </div>
              {% endif %}
            {% else %}
              <!-- Contenuto di esempio per {{ link.title }} -->
              <div class="formatted-content">
                {% case link.title %}
                  {% when 'Eventi' %}
                    <h3>Programma Eventi</h3>
                    <div class="event-card">
                      <h4 class="christmas-accent">Spettacolo di Luci</h4>
                      <p>Ogni sera alle <span class="highlight-text">18:00</span> e <span class="highlight-text">20:00</span></p>
                      <p>Un magico spettacolo di luci sincronizzate con musiche natalizie che incanterà grandi e piccini.</p>
                    </div>
                    
                    <div class="event-card">
                      <h4 class="christmas-accent">Laboratori per Bambini</h4>
                      <p>Tutti i weekend dalle <span class="highlight-text">15:00 alle 17:00</span></p>
                      <ul>
                        <li>Decorazioni natalizie fai-da-te</li>
                        <li>Letterine a Babbo Natale</li>
                        <li>Biscotti di pan di zenzero</li>
                      </ul>
                      <a href="#" class="family-cta">Prenota il laboratorio</a>
                    </div>
                    
                  {% when 'Biglietti' %}
                    <h3>Tariffe e Biglietti</h3>
                    <div class="info-grid">
                      <div class="info-box">
                        <h4>Adulti</h4>
                        <p class="price-tag">€15</p>
                      </div>
                      <div class="info-box">
                        <h4>Bambini (3-12)</h4>
                        <p class="price-tag">€10</p>
                      </div>
                      <div class="info-box">
                        <h4>Famiglia (2+2)</h4>
                        <p class="price-tag">€40</p>
                      </div>
                    </div>
                    <div class="event-card" style="text-align: center; margin-top: 2rem;">
                      <p>I biglietti includono accesso a tutti gli spettacoli e attività</p>
                      <a href="#" class="family-cta">Acquista Online</a>
                    </div>
                    
                  {% when 'Info', 'Informazioni' %}
                    <h3>Informazioni Pratiche</h3>
                    <div class="event-card">
                      <h4 class="christmas-accent">Orari di Apertura</h4>
                      <p><strong>Dicembre:</strong> Tutti i giorni 15:00 - 21:00</p>
                      <p><strong>Weekend:</strong> 10:00 - 22:00</p>
                      <p><strong>24 Dicembre:</strong> 10:00 - 18:00</p>
                    </div>
                    
                    <div class="event-card">
                      <h4 class="christmas-accent">Come Raggiungerci</h4>
                      <p>📍 Piazza Centrale, Centro Storico</p>
                      <p>🚇 Metro: Linea A, fermata Duomo</p>
                      <p>🚌 Bus: Linee 23, 45, 67</p>
                      <p>🚗 Parcheggio convenzionato disponibile</p>
                    </div>
                    
                  {% else %}
                    <div class="event-card">
                      <h3 class="christmas-accent">{{ link.title }}</h3>
                      <p>Benvenuti nella sezione <span class="highlight-text">{{ link.title }}</span> di Lucine di Natale!</p>
                      <p>Un'esperienza magica per tutta la famiglia, dove tradizione e innovazione si incontrano per creare ricordi indimenticabili.</p>
                      <ul>
                        <li>Atmosfere incantate</li>
                        <li>Attività per tutte le età</li>
                        <li>Sapori della tradizione</li>
                        <li>Shopping natalizio</li>
                      </ul>
                      <p>Scopri tutto quello che <span class="highlight-text">Lucine di Natale</span> ha da offrirti!</p>
                    </div>
                {% endcase %}
              </div>
            {% endif %}
            
          {% elsif link.url contains '/collections/' %}
            {% assign collection_handle = link.url | split: '/collections/' | last %}
            {% assign collection = collections[collection_handle] %}
            
            {% if collection.description != blank %}
              <p>{{ collection.description }}</p>
            {% endif %}
            
            {% for product in collection.products limit: 3 %}
              <div class="event-card">
                <h3 class="christmas-accent">{{ product.title }}</h3>
                {% if product.featured_image %}
                  <img src="{{ product.featured_image | img_url: '400x300' }}" alt="{{ product.title }}" style="width: 100%; border-radius: 10px; margin: 1rem 0;">
                {% endif %}
                <p>{{ product.description | truncate: 200 }}</p>
                {% if product.price %}
                  <p class="highlight-text">Da {{ product.price | money }}</p>
                {% endif %}
                <a href="{{ product.url }}" class="family-cta">Scopri di più</a>
              </div>
            {% endfor %}
            
          {% else %}
            <div class="event-card">
              <h3 class="christmas-accent">{{ link.title }}</h3>
              <p>Una sezione speciale dedicata alle <span class="highlight-text">famiglie</span> che vogliono vivere insieme la magia del Natale.</p>
              <p>Scopri le nostre <span class="highlight-text">Lucine di Natale</span> e lasciati incantare dall'atmosfera festosa pensata per grandi e piccini.</p>
            </div>
          {% endif %}
        </div>
      </div>
    {% endfor %}
  </div>
</div>

<!-- BACK TO TOP BUTTON -->
<button id="back-to-top" onclick="scrollToTop()">↑</button>