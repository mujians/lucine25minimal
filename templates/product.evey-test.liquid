{% layout none %}
<!doctype html>
<html>
<head>
  <title>Test Evey - {{ product.title }}</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  {{ content_for_header }}
  <style>
    body { margin: 0; padding: 20px; font-family: Arial, sans-serif; background: #f5f5f5; }
    .test-container { max-width: 800px; margin: 0 auto; background: white; padding: 20px; border-radius: 8px; }
    h1 { color: #333; }
    .evey-container { border: 2px solid #ddd; padding: 20px; margin: 20px 0; }
  </style>
</head>
<body>
  <div class="test-container">
    <h1>Test Evey Block - {{ product.title }}</h1>
    <p>Template pulito senza layout del tema</p>
    
    <div class="evey-container">
      <h3>Evey App Block (deve essere aggiunto nel template editor):</h3>
      <p>Vai nell'editor del tema e aggiungi il blocco Evey qui sotto</p>
      
      <!-- Section per app block -->
      <div id="evey-test-section">
        {% section 'evey-clean-test' %}
      </div>
    </div>
  </div>
</body>
</html>